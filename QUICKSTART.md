# 🚀 Швидкий старт для роботи над локалізацією

## 📋 Перш ніж почати

### 1. Ознайомтесь з документацією:
- **[.claude](.claude)** - Основна конфігурація проекту
- **[PROJECT_STATUS.md](.claude_docs/PROJECT_STATUS.md)** - Детальний статус, що вже зроблено
- **[TRANSLATION_GUIDE.md](.claude_docs/TRANSLATION_GUIDE.md)** - Повне керівництво з перекладу

### 2. Переконайтесь що маєте доступ до:
```
✓ /mnt/c/Users/zfurm/Documents/Paradox Interactive/Europa Universalis V/mod/ukrainian_localization/
✓ /mnt/d/SteamLibrary/steamapps/common/Europa Universalis V/game/
```

---

## ⚡ Швидкий старт (5 хвилин)

### Крок 1: Оберіть файл для роботи
```bash
cd "main_menu/localization/english"

# Подивитись які файли потребують виправлення
for f in *.yml; do
    count=$(grep -o '\[[a-z_]*|e\]' "$f" 2>/dev/null | wc -l)
    if [ $count -gt 0 ] && [ $count -lt 100 ]; then
        echo "$count $f"
    fi
done | sort -n
```

Оберіть файл з невеликою кількістю форматів (< 100) для початку.

---

### Крок 2: Автоматичне виправлення (3 команди)
```bash
# Припустимо обрали: scripted_lists_l_english.yml (96 форматів)

# 1. Застосувати відомі переклади
python3 ../../scripts/fix_any_file_universal.py scripted_lists_l_english.yml

# 2. Доповнити з російського
python3 ../../scripts/auto_translate_from_russian.py scripted_lists_l_english.yml

# 3. Додати _with_icon варіанти
python3 ../../scripts/fix_hints_with_icon.py
```

---

### Крок 3: Перевірка результату
```bash
# Скільки залишилось?
grep -o '\[[a-z_]*|e\]' scripted_lists_l_english.yml | wc -l

# Які саме?
grep -o '\[[a-z_]*|e\]' scripted_lists_l_english.yml | sort -u
```

Зазвичай після 3 скриптів залишається 0-10 концептів.

---

### Крок 4: Доопрацювання (якщо потрібно)

Якщо залишились концепти, додайте їх вручну:

```bash
# Приклад для 3 концептів:
sed -i \
"s/\[concept1|e\]/[Concept('concept1','переклад1')|e]/g; \
s/\[concept2|e\]/[Concept('concept2','переклад2')|e]/g; \
s/\[concept3|e\]/[Concept('concept3','переклад3')|e]/g" \
scripted_lists_l_english.yml
```

**Як вибрати правильний відмінок:**
1. Знайдіть контекст використання: `grep -C3 "\[concept1|e\]" scripted_lists_l_english.yml`
2. Подивіться яке слово перед концептом (вашу, нашу, призначити, тощо)
3. Оберіть відповідний відмінок

---

### Крок 5: Фінальна перевірка
```bash
# Має показати 0
grep -o '\[[a-z_]*|e\]' scripted_lists_l_english.yml | wc -l

# Готово! ✓
```

---

## 📝 Приклад повного циклу

```bash
# 1. Перейти в робочу директорію
cd "/mnt/c/Users/zfurm/Documents/Paradox Interactive/Europa Universalis V/mod/ukrainian_localization/main_menu/localization/english"

# 2. Перевірити початковий стан
echo "=== subject_interactions_l_english.yml ==="
grep -o '\[[a-z_]*|e\]' subject_interactions_l_english.yml | wc -l
# Показує: 92

# 3. Автоматичне виправлення
python3 ../../scripts/fix_any_file_universal.py subject_interactions_l_english.yml
# Результат: 92 → 30

python3 ../../scripts/auto_translate_from_russian.py subject_interactions_l_english.yml
# Результат: 30 → 5

# 4. Перевірити що залишилось
grep -o '\[[a-z_]*|e\]' subject_interactions_l_english.yml | sort -u
# Показує 5 концептів

# 5. Доопрацювати вручну
sed -i \
"s/\[concept_a|e\]/[Concept('concept_a','переклад_а')|e]/g; \
s/\[concept_b|e\]/[Concept('concept_b','переклад_б')|e]/g; \
s/\[concept_c|e\]/[Concept('concept_c','переклад_в')|e]/g; \
s/\[concept_d|e\]/[Concept('concept_d','переклад_г')|e]/g; \
s/\[concept_e|e\]/[Concept('concept_e','переклад_д')|e]/g" \
subject_interactions_l_english.yml

# 6. Фінальна перевірка
grep -o '\[[a-z_]*|e\]' subject_interactions_l_english.yml | wc -l
# Показує: 0 ✓

echo "✅ Файл subject_interactions_l_english.yml повністю виправлено!"
```

**Час:** ~5-10 хвилин

---

## 🎯 Рекомендовані файли для початку

### Легкі (< 100 форматів, ~5-10 хв):
```bash
scripted_lists_l_english.yml        # 96
subject_interactions_l_english.yml  # 92
auto_modifiers_l_english.yml        # 90
parliament_l_english.yml            # 79
disasters_l_english.yml             # 78
```

### Середні (100-600, ~15-30 хв):
```bash
tutorial_l_english.yml              # 556
lists_l_english.yml                 # 509
scripted_triggers_l_english.yml     # 520
```

### Складні (> 1000, ~1-2 год):
```bash
messages_l_english.yml              # 1378
interfaces_l_english.yml            # 2478
modifier_types_l_english.yml        # 3885
triggers_l_english.yml              # 4039
```

**Рекомендація:** Почніть з легких файлів щоб звикнути до процесу.

---

## 💡 Корисні поради

### 1. Як визначити правильний відмінок:

**Шукайте контекст:**
```bash
grep -B2 -A2 "\[country|e\]" filename.yml
```

**Приклади:**
- "вашу [country|e]" → знахідний відмінок → "вашу країну"
- "країни [country|e]" → родовий відмінок → "країни країни"
- "призначити [cabinet_member|e]" → знахідний → "призначити члена кабінету"

### 2. Частi помилки:

❌ **Неправильно:** Копіювання з називного відмінку
```yaml
[Concept('country','країна')|e]  # у контексті "вашу ___"
# Результат: "вашу країна" - неправильно!
```

✅ **Правильно:** Використання потрібного відмінку
```yaml
[Concept('country','країну')|e]  # у контексті "вашу ___"
# Результат: "вашу країну" - правильно!
```

### 3. Використовуйте базу знань:

Перевірте чи є переклад у вже виправлених файлах:
```bash
grep "Concept('country'," ../../main_menu/localization/english/*.yml
```

---

## 📊 Відстеження прогресу

### Скільки файлів залишилось:
```bash
cd main_menu/localization/english
for f in *.yml; do
    count=$(grep -o '\[[a-z_]*|e\]' "$f" 2>/dev/null | wc -l)
    if [ $count -gt 0 ]; then
        echo "$count $f"
    fi
done | wc -l
```

### Загальна статистика:
```bash
# Всього старих форматів у всіх файлах
for f in *.yml; do
    grep -o '\[[a-z_]*|e\]' "$f" 2>/dev/null
done | wc -l
```

---

## 🆘 Якщо щось пішло не так

### Скрипт не працює:
```bash
# Перевірити Python
python3 --version  # Має бути 3.6+

# Перевірити шлях до скриптів
ls -la ../../scripts/
```

### Файл не знайдено:
```bash
# Перевірити поточну директорію
pwd

# Має бути:
# /mnt/c/Users/.../ukrainian_localization/main_menu/localization/english
```

### Не впевнені в перекладі:
1. Подивіться російський файл: `/mnt/d/SteamLibrary/.../russian/filename_l_russian.yml`
2. Перевірте PROJECT_STATUS.md - список частих перекладів
3. Перевірте TRANSLATION_GUIDE.md - повне керівництво

---

## ✅ Чеклист перед завершенням

- [ ] Запустив всі 3 скрипти
- [ ] Перевірив що `grep -o '\[[a-z_]*|e\]' file.yml | wc -l` = 0
- [ ] Переклади використовують правильні відмінки
- [ ] Немає великих літер без потреби
- [ ] Немає російських слів
- [ ] Зробив backup (якщо є сумніви)

---

**Готові почати? Обирайте файл і вперед! 🚀**

Питання? Дивіться:
- [TRANSLATION_GUIDE.md](.claude_docs/TRANSLATION_GUIDE.md) - Детальне керівництво
- [scripts/README.md](scripts/README.md) - Документація скриптів
