# 🛠️ Інструменти для перекладу локалізації EU5

Цей набір інструментів дозволяє ефективно перекладати великі YAML файли локалізації, розділяючи їх на частини для паралельної обробки.

## 📦 Інструменти

### 1. `split_for_translation.py` - Розділення файлу
Розділяє великий YAML файл на менші частини для зручного перекладу.

**Використання:**
```bash
python3 tools/split_for_translation.py <input_file> [options]
```

**Параметри:**
- `input_file` - вхідний YAML файл (обов'язковий)
- `--tokens N` - макс. розмір частини в токенах (за замовчуванням: 15000)
- `--output-dir DIR` - папка для збереження частин

**Приклади:**
```bash
# Базове використання
python3 tools/split_for_translation.py main_menu/localization/english/events_l_english.yml

# З власними параметрами
python3 tools/split_for_translation.py file.yml --tokens 20000 --output-dir my_chunks

# Для різних файлів
python3 tools/split_for_translation.py main_menu/localization/english/government_l_english.yml
python3 tools/split_for_translation.py main_menu/localization/english/modifiers_l_english.yml
```

**Що створюється:**
- `<name>_chunks/chunk_001.txt`, `chunk_002.txt`, ... - частини файлу
- `<name>_chunks/metadata.json` - метадані (вхідний файл, кількість частин, тощо)
- `<name>_chunks/ІНСТРУКЦІЯ.txt` - інструкція з перекладу

---

### 2. `merge_translation.py` - Об'єднання частин
Об'єднує перекладені частини назад в один YAML файл.

**Використання:**
```bash
python3 tools/merge_translation.py <chunks_dir> [options]
```

**Параметри:**
- `chunks_dir` - папка з перекладеними частинами (обов'язковий)
- `--output FILE` - вихідний файл (за замовчуванням з metadata.json)
- `--suffix NAME` - суфікс перекладених файлів (за замовчуванням: fixed)

**Приклади:**
```bash
# Базове використання (шукає chunk_*_fixed.txt)
python3 tools/merge_translation.py events_chunks

# З власним суфіксом (chunk_*_translated.txt)
python3 tools/merge_translation.py chunks --suffix translated

# З власним вихідним файлом
python3 tools/merge_translation.py chunks --output result.yml
```

**Що створюється:**
- Об'єднаний YAML файл
- `merge_report.txt` - звіт про об'єднання

---

### 3. `validate_translation.py` - Валідація перекладу
Перевіряє якість та правильність перекладеного файлу.

**Використання:**
```bash
python3 tools/validate_translation.py <file.yml> [options]
```

**Параметри:**
- `file` - файл для перевірки (обов'язковий)
- `--strict` - строгий режим (попередження = помилки)

**Приклади:**
```bash
# Базова валідація
python3 tools/validate_translation.py main_menu/localization/english/events_l_english.yml

# Строга валідація
python3 tools/validate_translation.py file.yml --strict
```

**Що перевіряється:**
- ✅ YAML синтаксис
- ✅ Правильність `Concept()` конструкцій
- ✅ Технічні теги (`$VALUE$`, `#T`, `#!`)
- ✅ Відсутність російських літер (ё, ъ)
- ✅ Наявність BOM маркера

---

## 🔄 Повний робочий процес

### Метод 1: Ручний переклад з Claude

```bash
# 1. Розділити файл
python3 tools/split_for_translation.py main_menu/localization/english/events_l_english.yml

# 2. Вручну перекласти кожен chunk_XXX.txt через Claude
#    Відкрити chunk_001.txt в Claude
#    Попросити: "Переклади цей файл українською з правильними Concept()"
#    Зберегти як chunk_001_fixed.txt
#    Повторити для всіх частин

# 3. Об'єднати перекладені частини
python3 tools/merge_translation.py events_chunks

# 4. Валідувати результат
python3 tools/validate_translation.py main_menu/localization/english/events_l_english.yml

# 5. Закомітити якщо все ОК
git add main_menu/localization/english/events_l_english.yml
git commit -m "Переклад events_l_english.yml"
git push
```

### Метод 2: Автоматизований переклад (паралельні агенти)

```python
# Використовуйте Task tool для запуску паралельних агентів
# Приклад див. у коміті з interfaces_l_english.yml
```

---

## 📝 Правила перекладу

### 1. **Concept() - НАЙВАЖЛИВІШЕ!**

Всі ігрові терміни мають бути обгорнуті в `Concept()` з **правильними відмінками**:

```yaml
# ❌ НЕПРАВИЛЬНО
TAB_TEXT: "Відкрити панель армії"
TAB_TEXT: "Інформація про [army|e]"

# ✅ ПРАВИЛЬНО
TAB_TEXT: "Відкрити панель [Concept('army','армії')|e]"
TAB_TEXT: "Інформація про [Concept('army','армію')|e]"
```

### 2. **Відмінки в Concept()**

Перевіряйте кожен іменник на правильний відмінок:

```yaml
# Родовий (кого? чого?) - до, від, з/із, без, для
"до [Concept('religion','віри')|e]"
"без [Concept('gold','золота')|e]"
"для [Concept('army','армії')|e]"

# Знахідний (кого? що?) - на (про), за, через, про
"інфо про [Concept('market','ринок')|e]"
"вибрати [Concept('character','персонажа')|e]"

# Орудний (ким? чим?) - з/із, керувати, володіти
"керувати [Concept('army','армією')|e]"
"з [Concept('navy','флотом')|e]"

# Місцевий (де?) - в/у, на
"у [Concept('province','провінції')|e]"
"на [Concept('market','ринку')|e]"

# Давальний (кому? чому?) - до (куди), дозволяти
"дозволити [Concept('country','державі')|e]"
```

### 3. **Технічні конструкції**

**ЗБЕРІГАЙТЕ** всі технічні елементи:
- `$VALUE$`, `$VAL|0$`, `$PERCENT|%$` - змінні
- `#T` та `#!` - теги форматування
- `[GetName]`, `[Market.GetName]` - функції
- `@gold!`, `@trigger_no!` - іконки
- `|e`, `|E` - кінцівки тегів

### 4. **Що НЕ робити**

❌ НЕ змінюйте англійські ключі (ліва частина):
```yaml
WRONG_KEY: "Текст"  # ❌ Не змінювати WRONG_KEY
```

❌ НЕ використовуйте російську кальку:
```yaml
# ❌ ПОГАНО
"Обчислення..."
"Нестача їжі"
"попів" (замість "працівників")

# ✅ ДОБРЕ
"Обчислюється..."
"Бракує їжі"
"працівників"
```

❌ НЕ використовуйте російські літери ё, ъ

---

## 🎯 Поради для ефективної роботи

### Для великих файлів (>5000 рядків)
- Використовуйте розмір чанка 15000 токенів
- Перекладайте паралельно (кілька чанків одночасно через різні вкладки Claude)
- Створіть словник термінів перед початком

### Для середніх файлів (2000-5000 рядків)
- Можна збільшити до 20000 токенів на чанк
- Перекладайте послідовно

### Для малих файлів (<2000 рядків)
- Можна перекладати одним запитом без розділення

### Перевірка якості
Завжди запускайте валідатор після перекладу:
```bash
python3 tools/validate_translation.py <your_file.yml>
```

---

## 🐛 Усунення проблем

### Помилка: "Не знайдено перекладених файлів"
- Переконайтеся, що ви зберегли файли як `chunk_XXX_fixed.txt`
- Або вкажіть інший суфікс: `--suffix translated`

### Помилка: "Незбалансовані теги #T та #!"
- Перевірте кожен рядок з помилкою
- Кожен `#T` має мати відповідний `#!`

### Попередження: "Відсутній BOM маркер"
- Файл потрібно зберігати з UTF-8 BOM
- Скрипти автоматично додають BOM при збереженні

---

## 📚 Додаткові ресурси

- **Приклад успішного перекладу:** див. коміт з `interfaces_l_english.yml`
- **Словник термінів:** `reference/terms_dictionary.json` (якщо є)
- **Довідка з відмінків:** [Ukrainian Grammar Guide](https://ukrainian.stackexchange.com/)

---

## 🤝 Внесок

При додаванні нових інструментів:
1. Додайте документацію в цей README
2. Додайте приклади використання
3. Оновіть список файлів у `.gitignore` якщо потрібно

---

**Створено для проєкту EU5 Ukrainian Localization**
**Автор: Claude + furmonenko**
**Версія: 1.0**
