# Claude Code Project Configuration

## Робоча директорія
/mnt/c/Users/zfurm/Documents/Paradox Interactive/Europa Universalis V/mod/ukrainian_localization/main_menu/localization/english

## Структура проекту

### Основні шляхи
- **Українська локалізація:** `/mnt/c/Users/zfurm/Documents/Paradox Interactive/Europa Universalis V/mod/ukrainian_localization/`
- **Російська локалізація (референс):** `reference/russian/` (487 файлів, 29MB) ✓ СКОПІЙОВАНО
  - 119 основних файлів локалізації
  - 300 файлів подій (events/)
  - 62 файли назв локацій (location_names/)
  - 4 файли місій (missions/)
  - 2 файли налагодження (qa_debug/)
- **Скрипти:**
  - `scripts/` - робочі скрипти для Concept() заміни (3 файли)
  - `tools/` - універсальний пайплайн для перекладу великих файлів (3 скрипти)

### Додатково (якщо є доступ):
- **Оригінальні файли гри:** `/mnt/d/SteamLibrary/steamapps/common/Europa Universalis V/game/`

### Важливо
Файли локалізації знаходяться в папці `english`, але містять **українські** переклади. Це обмеження гри - заголовок завжди `l_english:`, навіть для української мови.

## Контекст проекту

### Мета
Переклад локалізації Europa Universalis V українською мовою з правильними граматичними формами.

### Поточний статус (детально в ПЕРЕКЛАД_ПРОГРЕС.md)
- ✅ Перекладено: **60 файлів** (55% основної локалізації, ~70,000 рядків)
- 🇬🇧 Залишилось: **44 файли** англійською
- 🇷🇺 Залишилось: **2 файли** російською
- 📝 Події: Почато переклад (кілька файлів з 300+)

### Основні досягнення
- Виправлено 537+ граматичних помилок
- Додано 441 новий ключ перекладу
- Видалено всі російські літери (ё, ъ) з локалізації
- Виправлено 130+ російських конструкцій
- Замінено "приватник" → "капер" у всій локалізації

## Інструменти та скрипти

### scripts/ (для Concept() замін)

**1. fix_any_file_universal.py**
- Використовує 440+ відомі переклади з уже виправлених файлів
- Застосовує автоматично до нових файлів
- Покриває 60-80% замін

**2. auto_translate_from_russian.py**
- Читає російський файл з `reference/russian/`
- Перекладає російське → українське через словник
- Покриває ще 15-30% замін
- Працює без встановленого EU5 (використовує локальні файли)

**3. fix_hints_with_icon.py**
- Розумно зіставляє _with_icon варіанти з базовими концептами
- Покриває ще 5-10% замін

### tools/ (універсальний пайплайн)

**1. split_for_translation.py**
- Розділяє великі YAML файли на частини для паралельного перекладу
- Зручно для величезних файлів (events, triggers, тощо)

**2. merge_translation.py**
- Об'єднує перекладені частини назад в один файл
- Перевіряє цілісність

**3. validate_translation.py**
- Валідація YAML синтаксису
- Перевірка Concept() конструкцій
- Пошук російських літер
- Перевірка BOM маркера

Детальна документація: `tools/README.md`, `tools/QUICKSTART.md`

### Допоміжні скрипти

**check_translation_status.py**
- Автоматична перевірка стану перекладу всіх файлів
- Визначає мову (українська/російська/англійська)
- Генерує звіти

## Правила роботи

### Завжди:
1. Працювати з UTF-8 BOM кодуванням
2. Використовувати правильні відмінки української мови
3. Малі літери для звичайних іменників (не власних назв)
4. Перевіряти контекст перед вибором відмінку
5. Уникати російських кальок

### Ніколи:
1. Не використовувати російські літери (ё, ъ, ы, э)
2. Не копіювати російські конструкції напряму
3. Не використовувати російські кальки ("терпимість" → "толерантність")

## База знань

### Приклади частих перекладів:
- `country` → "країна" / "країну" / "країни"
- `pops` → "населення" (не "попи"!)
- `war` → "війна" / "війну" / "війни"
- `army` → "армія" / "армію" / "армії"
- `cabinet_member` → "член кабінету" / "члена кабінету"
- `tolerated_culture` → "толерантна культура" (не "терпима"!)

### Документація:
- **ПЕРЕКЛАД_ПРОГРЕС.md** - Повний звіт про прогрес перекладу (60/109 файлів)
  - Список перекладених файлів з рейтингами
  - Список неперекладених з пріоритетами
  - Статистика по рядках та файлах
  - Рекомендований план перекладу

## Швидкі команди

### Перевірка статусу:
```bash
# Перевірити всі файли
python3 check_translation_status.py

# Розмір проекту
du -sh reference/ scripts/ tools/
```

### Переклад нового файлу (tools пайплайн):
```bash
# Розділити великий файл на частини
python3 tools/split_for_translation.py main_menu/localization/english/file.yml

# Після перекладу - об'єднати
python3 tools/merge_translation.py file_chunks

# Валідація
python3 tools/validate_translation.py main_menu/localization/english/file.yml
```

## Пріоритети перекладу

### Високий пріоритет (7 файлів):
- lists_l_english.yml (1,100 рядків)
- goods_l_english.yml (962)
- government_names_l_english.yml (946)
- international_organizations_l_english.yml (871)
- scripted_effects_l_english.yml (840)
- situations_l_english.yml (758)
- opinions_l_english.yml (754)

Повний список: див. ПЕРЕКЛАД_ПРОГРЕС.md

## Технічні деталі

- **Формат:** YAML з UTF-8 BOM
- **Заголовок:** `l_english:` (навіть для української!)
- **Макроси:** `[Concept()]`, `$VAR$`, `#TAG#` - не змінювати
- **Кольори:** `#G`, `#R`, `#W`, `#Y`, `#L` - не змінювати
- **Git:** Проект є git-репозиторієм

---

**Останнє оновлення:** 2025-11-18
**Версія:** 2.0 (після очищення проекту)
