# Керівництво з перекладу EU5 на українську

## 🎯 Основна мета проекту

Виправити українську локалізацію Europa Universalis V, щоб вона правильно використовувала відмінки та граматику української мови.

## 📖 Формати локалізації

### Старий формат (проблемний)
```yaml
# game_concepts_l_english.yml
game_concept_country: "Країна"

# Використання в інших файлах:
tooltip: "У цьому вікні ви можете бачити [country|e]"
# Результат: "У цьому вікні ви можете бачити Країна" ❌
```

**Проблема:** Завжди показує називний відмінок з великої літери.

### Новий формат (правильний)
```yaml
tooltip: "У цьому вікні ви можете бачити [Concept('country','країну')|e]"
# Результат: "У цьому вікні ви можете бачити країну" ✅
```

**Переваги:**
- Можна вказати будь-який відмінок
- Малі літери (як у звичайному тексті)
- Гнучкість для різних контекстів

## 🔧 Процес виправлення файлу

### Крок 1: Аналіз файлу
```bash
cd "/mnt/c/Users/zfurm/Documents/Paradox Interactive/Europa Universalis V/mod/ukrainian_localization/main_menu/localization/english"

# Скільки старих форматів?
grep -o '\[[a-z_]*|e\]' filename_l_english.yml | wc -l

# Які найчастіші?
grep -o '\[[a-z_]*|e\]' filename_l_english.yml | sort | uniq -c | sort -rn | head -20
```

### Крок 2: Автоматичне виправлення
```bash
# Етап 1: Використати вже відомі переклади (з виправлених файлів)
python3 /tmp/fix_any_file_universal.py filename_l_english.yml

# Етап 2: Доповнити з російського файлу
python3 /tmp/auto_translate_from_russian.py filename_l_english.yml

# Етап 3: Додати _with_icon варіанти
python3 /tmp/fix_hints_with_icon.py
```

### Крок 3: Перевірка залишків
```bash
# Що залишилось?
grep -o '\[[a-z_]*|e\]' filename_l_english.yml | sort -u
```

### Крок 4: Ручне доопрацювання
Для концептів що залишились, додати переклади вручно:

```bash
# Приклад для одного концепту
sed -i "s/\[concept_name|e\]/[Concept('concept_name','переклад_у_відмінку')|e]/g" filename_l_english.yml

# Приклад для багатьох
cd english_folder && sed -i \
"s/\[concept1|e\]/[Concept('concept1','переклад1')|e]/g; \
s/\[concept2|e\]/[Concept('concept2','переклад2')|e]/g; \
s/\[concept3|e\]/[Concept('concept3','переклад3')|e]/g" filename_l_english.yml
```

### Крок 5: Фінальна перевірка
```bash
# Має показати 0
grep -o '\[[a-z_]*|e\]' filename_l_english.yml | wc -l
```

## 📚 Словник базових перекладів

### Основні концепти
| Англійська | Називний | Знахідний | Родовий | Множина |
|------------|----------|-----------|---------|---------|
| country | країна | країну | країни | країни |
| ruler | правитель | правителя | правителя | правителі |
| war | війна | війну | війни | війни |
| army | армія | армію | армії | армії |
| navy | флот | флот | флоту | флоти |
| pops | населення | населення | населення | населення |
| building | будівля | будівлю | будівлі | будівлі |
| location | локація | локацію | локації | локації |
| province | провінція | провінцію | провінції | провінції |
| religion | релігія | релігію | релігії | релігії |
| culture | культура | культуру | культури | культури |
| market | ринок | ринок | ринку | ринки |

### Державні структури
- `cabinet` → "кабінет"
- `cabinet_member` → "член кабінету" / "члена кабінету" (знахідний)
- `parliament` → "парламент"
- `government` → "уряд"
- `estates` → "стани"

### Військові терміни
- `levies` → "ополчення"
- `manpower` → "людські ресурси"
- `siege` → "облога"
- `battle` → "битва"
- `war_goal` → "військова мета"

### Дипломатія
- `alliance` → "альянс"
- `rival` → "суперник"
- `subject` → "васал"
- `overlord` → "сюзерен"
- `casus_belli` → "привід для війни"

### Економіка
- `goods` → "товари"
- `trade` → "торгівля"
- `gold` → "золото"
- `tax` → "податок"
- `income` → "дохід"

## 🎨 Правила перекладу

### 1. Відмінки
Завжди використовувати той відмінок, який підходить за контекстом:

```yaml
# ❌ Неправильно
"вашу [country|e]" → "вашу Країна"

# ✅ Правильно
"вашу [Concept('country','країну')|e]" → "вашу країну"

# ✅ Інший приклад
"країни [Concept('country','країни')|e]" → "країни країни" (родовий відмінок)
```

### 2. Великі та малі літери
У середині речення завжди використовувати малу літеру (якщо це не власна назва):

```yaml
# ❌ Неправильно
'Країна', 'Армія', 'Війна'

# ✅ Правильно
'країна', 'армія', 'війна'
```

### 3. Однина та множина
Для множини використовувати правильну форму:

```yaml
[Concept('countries','країни')|e]  # не "країнs"
[Concept('armies','армії')|e]      # не "армійs"
[Concept('wars','війни')|e]        # не "війнs"
```

### 4. Концепти з _with_icon
Використовувати той же переклад що й без _with_icon:

```yaml
[Concept('market','ринок')|e]
[Concept('market_with_icon','ринок')|e]  # той самий переклад
```

### 5. Уникати калькування з російської
```yaml
# ❌ Російське калькування
"терпимість" (від російського "терпимость")

# ✅ Правильне українське слово
"толерантність"
```

## 🔍 Частi помилки та як їх уникнути

### Помилка 1: Не врахували контекст
```yaml
# Контекст: "призначити [cabinet_member|e]"
# ❌ Неправильно:
[Concept('cabinet_member','член кабінету')|e]  → "призначити член кабінету"

# ✅ Правильно (знахідний відмінок):
[Concept('cabinet_member','члена кабінету')|e] → "призначити члена кабінету"
```

### Помилка 2: Копіювання з називного відмінку
```yaml
# У game_concepts є: game_concept_country: "країна"
# Контекст: "вашу [country|e]"

# ❌ Неправильно (просто скопіювали):
[Concept('country','країна')|e]  → "вашу країна"

# ✅ Правильно (знахідний):
[Concept('country','країну')|e]  → "вашу країну"
```

### Помилка 3: Неправильний переклад терміну
```yaml
# ❌ Неправильно:
'Попи' для pops (це священики!)

# ✅ Правильно:
'населення' для pops (це групи населення в грі)
```

## 🛠️ Додаткові утиліти

### Швидка заміна для списку концептів
Створити Python скрипт:

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-

filepath = "path/to/file_l_english.yml"

REPLACEMENTS = {
    '[concept1|e]': "[Concept('concept1','переклад1')|e]",
    '[concept2|e]': "[Concept('concept2','переклад2')|e]",
    # ... додати всі потрібні
}

with open(filepath, 'r', encoding='utf-8-sig') as f:
    content = f.read()

for old, new in REPLACEMENTS.items():
    content = content.replace(old, new)

with open(filepath, 'w', encoding='utf-8-sig') as f:
    f.write(content)

print("✓ Готово!")
```

### Пошук контексту для визначення відмінку
```bash
# Знайти всі використання концепту country
grep -n "\[country|e\]" filename_l_english.yml

# Подивитись контекст (5 рядків до і після)
grep -C5 "\[country|e\]" filename_l_english.yml
```

## 📝 Чеклист перед комітом

- [ ] Всі старі формати `[concept|e]` замінено на `[Concept('concept','translation')|e]`
- [ ] Перевірено що `grep -o '\[[a-z_]*|e\]' file.yml | wc -l` повертає 0
- [ ] Переклади використовують правильні відмінки
- [ ] Використані малі літери (крім власних назв)
- [ ] Немає російських слів (перевірити "терпим*", "попи", тощо)
- [ ] Файл зберігається у UTF-8 BOM кодуванні
- [ ] Перевірено в грі (якщо можливо)

## 🚀 Швидкий старт для нового файлу

```bash
# 1. Перейти в робочу директорію
cd "/mnt/c/Users/zfurm/Documents/Paradox Interactive/Europa Universalis V/mod/ukrainian_localization/main_menu/localization/english"

# 2. Перевірити початковий стан
echo "=== Файл: your_file_l_english.yml ==="
grep -o '\[[a-z_]*|e\]' your_file_l_english.yml | wc -l

# 3. Автоматичне виправлення
python3 /tmp/fix_any_file_universal.py your_file_l_english.yml
python3 /tmp/auto_translate_from_russian.py your_file_l_english.yml

# 4. Перевірити що залишилось
grep -o '\[[a-z_]*|e\]' your_file_l_english.yml | sort -u

# 5. Доробити вручну останні концепти
# (використати sed або Python скрипт)

# 6. Фінальна перевірка
grep -o '\[[a-z_]*|e\]' your_file_l_english.yml | wc -l
# Має показати 0
```

## 💾 Резервне копіювання

Перед масовими змінами завжди робити бекап:

```bash
# Створити копію файлу
cp your_file_l_english.yml your_file_l_english.yml.backup

# Або всієї директорії
cd "/mnt/c/Users/zfurm/Documents/Paradox Interactive/Europa Universalis V/mod"
tar -czf ukrainian_localization_backup_$(date +%Y%m%d).tar.gz ukrainian_localization/
```
